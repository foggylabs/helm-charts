# Patterns to ignore when building packages.
.DS_Store
.git/
.gitignore
.vscode/
.idea/
*.tmproj
*.swp
*.bak
*.tmp
*.orig
*~
# CI and tooling — tests / CI belong in the repo, not in the packaged chart.
.github/
ci/
tests/
