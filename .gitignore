# OS / editor
.DS_Store
.idea/
.vscode/
*.swp

# env file
.env

# Helm — packaged tarballs and downloaded subcharts.
# Chart.lock IS committed (pins dep versions). The downloaded .tgz files under
# charts/*/charts/ are not — CI runs `helm dependency update` on every build.
*.tgz
charts/*/charts/
